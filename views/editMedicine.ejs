<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Medicine</title>
</head>
<body>
    <h1>
        Edit Medicine
    </h1>
    <form action="/main/admin/edit/<%= medicine.id %>" method="post">
        <label for="name">Medicine Name</label>
        <input type="text" name="name" value="<%= medicine.name %>">
        <br><br>
        <label for="description">Medicine Description</label>
        <textarea name="description" id="" cols="30" rows="10"><%= medicine.description %></textarea>
        <br><br>

        <label for="price">Medicine Price</label>
        <input type="text" name="price" value="<%= medicine.price %>">
        <br><br>

        <label for="imageUrl">Medicine Image Url</label>
        <input type="text" name="imageUrl" value="<%= medicine.imageUrl %>">
        <br><br>

        <label for="category">Category</label>
        <br><br>
        <select name="category" id="category">
            <option value="Dewasa" <%= medicine.category === "Dewasa" ? 'selected' : '' %>>Dewasa</option>
            <option value="Anak" <%= medicine.category === "Anak" ? 'selected' : '' %>>Anak</option>
        </select>
        <br><br>
        <button type="submit">Submit</button>
    </form>
</body>
</html>